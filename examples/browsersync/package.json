{
  "name": "@ui5/cli-example-browsersync",
  "version": "0.0.0",
  "private": true,
  "description": "UI5 CLI Example - Browsersync",
  "scripts": {
    "start": "concurrently \"npm:serve\" \"npm:browser-sync\"",
    "serve": "ui5 serve --port=8080",
    "browser-sync": "browser-sync start --proxy 'http://localhost:8080' --files 'webapp' --no-open"
  },
  "keywords": [],
  "author": "SAP SE (https://www.sap.com)",
  "license": "Apache-2.0",
  "devDependencies": {
    "@ui5/cli": "^1.2.1",
    "browser-sync": "^2.26.3",
    "concurrently": "^4.1.0"
  }
}
